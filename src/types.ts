export interface User {
  displayName: string | null;
  email: string | null;
  photoURL: string | null;
  id: string;
}

// interface Experiment {
//   id: string;
//   recordingId: string;
//   segments: number[][];
// }
